import React, { useEffect, useState } from "react";
import {
  View,
  ScrollView,
  Text,
  Image,
  StyleSheet,
  TouchableOpacity,
} from "react-native";
import { Button } from "react-native-paper";

const TermsAndPolicies = ({ route, navigation }) => {
  

  return (
    <ScrollView >

        <Text>This is terms and policies page</Text>
    </ScrollView>
  );
};


export default TermsAndPolicies;

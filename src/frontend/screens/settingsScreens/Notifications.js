import React, { useEffect, useState } from "react";
import {
  View,
  ScrollView,
  Text,
  Image,
  StyleSheet,
  TouchableOpacity,
} from "react-native";
import { Button } from "react-native-paper";

const Notifications = ({ route, navigation }) => {
  

  return (
    <ScrollView >

        <Text>This is edit notifications page</Text>
    </ScrollView>
  );
};


export default Notifications;
